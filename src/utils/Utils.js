class Utils {
  static getCurrentYear() {
    const currentDate = new Date();
    const currentYear = currentDate.getFullYear();
    return currentYear;
  }

  // You can add more utility functions here as needed in the future
}

export default Utils;